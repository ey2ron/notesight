# Server configuration
PORT=4000
HOST=0.0.0.0

# Path to the Oemer CLI executable. Override if it is not on the PATH.
# OEMER_BIN=oemer

# Path to the Python interpreter used for MIDI conversion.
# PYTHON_BIN=python

# Optional: override storage folder locations
# STORAGE_DIR=./storage
# DATA_DIR=./data
